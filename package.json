{
  "name": "word-coach",
  "private": true,
  "scripts": {
    "watch": "lerna run watch --no-sort --stream --concurrency 999",
    "build": "lerna run build --no-sort --stream --concurrency 999",
    "clean:node_modules": "lerna clean --yes",
    "clean:dist": "lerna run clean"
  },
  "devDependencies": {
    "lerna": "^6.5.1"
  },
  "engines": {
    "node": "^18.0.0"
  }
}